<div class="page-header page-header-default">
	<div class="page-header-content">
		<div class="page-title">
			<h4><i class="icon-arrow-left52 position-left"></i> <span class="text-semibold">{{ $title }}</span> @if(count($segment)>0) @foreach($segment as $segments) - {!!$segments!!} @endforeach @endif </h4>
		</div>
	</div>
</div>